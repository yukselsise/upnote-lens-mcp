[project]
name = "upnote-lens-mcp"
version = "0.1.2"
description = "Let AI read, search, and summarize your UpNote notes — and create new ones — right from your chat."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [{ name = "elsboo" }]
keywords = ["mcp", "upnote", "notes", "sqlite"]
dependencies = ["mcp>=1.2.0,<2"]

[project.urls]
Homepage = "https://github.com/AwesomeHye/upnote-lens-mcp"

[project.scripts]
upnote-lens-mcp = "upnote_lens.server:main"
upnote-lens = "upnote_lens.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/upnote_lens"]
